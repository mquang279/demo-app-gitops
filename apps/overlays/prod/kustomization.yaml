apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: prod

resources:
  - namespace.yaml
  - ingress.yaml
  - backend-ingress.yaml
  - ../../base

patches:
  - path: patches/backend-rollout-patch.yaml
  - path: patches/frontend-rollout-patch.yaml
