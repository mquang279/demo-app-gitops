apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argo

resources:
  - namespace.yaml
  - https://github.com/argoproj/argo-workflows/releases/download/v4.0.6/install.yaml

patches:
  - path: argo-server-auth-mode.yaml